name: network_request
description: A powerful and comprehensive Dart package for simplifying HTTP requests and interacting with RESTful APIs
version: 0.3.1
repository: https://github.com/ZakaTiwana/network_request

topics:
 - http
 - network
 - rest
 - logging
 - curl

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.6.0
  collection: ^1.19.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
